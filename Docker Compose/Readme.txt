Here i store some screenshots of the image where easily explain how create the command of docker compose file.Docker compose file is an special type of file in an docker image where i explain name of the container image of that container, all environment variables which used in the container with their values, port  by where inside the container or in the system run so that when an user use this image to create a container they does not mention that repeatedly

the extension of the file where we write this file this extension will be 'yaml' like python programming languages here also indention must be follow properly


in my this project the mongo-express container will not used

docker-compose is an demo yaml file put here for understanding purpose